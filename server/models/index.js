const db = require('./mysqldb.js');

module.exports = {
  getHighScores: (callback) => {
    // db.query('SELECT * FROM glossarylist', (err, results) => {
    //   // console.log('getting', err, results);
    //   if (err) {
    //     callback(err, null);
    //   }
    //   callback(null, results);
    // })
  }
}