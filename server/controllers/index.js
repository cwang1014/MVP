const { getHighScores } = require('../models');

module.exports = {
  // loadScores: (req, res) => {
  //   getHighScores((err, results) => {
  //     if (err) {
  //       res.status(404).end();
  //     }
  //     res.status(200).json(results);
  //   })
  // }
}